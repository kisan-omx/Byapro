import { Text, View } from "react-native";

export default function Auth() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-lg text-primary font-bold">Auth</Text>
    </View>
  );
}