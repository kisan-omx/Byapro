import { Text, View } from "react-native";

export default function Transactions() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-lg text-primary font-bold">Transactions</Text>
    </View>
  );
}